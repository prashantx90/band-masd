This is enough. I don't need the remaining component details.

After reviewing all 8 screens, I'd recommend **NOT modeling the UI**. Model the workflow engine. The screens are actually very consistent and point to a clean backend.

---

# My Verdict

You can build the entire hackathon backend using only **8 Directus Collections**.

The screens consistently revolve around:

- Projects
- Workflows
- Agents
- Agent Runs
- Messages
- Requirements
- Findings
- Reports

The Dashboard, Projects, Workflow, Activity Feed, Console, Reports and New Project pages all derive from those entities.

---

# Directus Collections

## projects

This is the root aggregate.

```yaml
projects
```

Fields:

```yaml
id: uuid
name: string
description: text
business_requirements: text
target_users: text
technology_preferences: text

status:
  draft
  running
  completed
  blocked

approval_status:
  pending
  approved
  rejected
  changes_requested

created_at
updated_at
```

Source:

New Project page + Projects page.

---

## workflows

One project may have multiple workflow executions.

```yaml
workflows
```

Fields:

```yaml
id
project_id

status:
  pending
  running
  completed
  failed

started_at
completed_at

current_agent
```

---

## agents

Static catalog.

```yaml
agents
```

Seed data:

```yaml
PM Agent
Architect Agent
Engineer Agent
QA Agent
Security Agent
Reviewer Agent
```

Source:

Workflow page.

---

## agent_runs

This is the most important collection.

Everything drives from here.

```yaml
agent_runs
```

Fields:

```yaml
id

workflow_id

agent_id

status:
  pending
  running
  completed
  failed

progress

started_at
completed_at

input_context
output_summary

tokens_in
tokens_out

execution_time_ms
```

This powers:

- Workflow page
- Console page
- Dashboard statistics

---

## messages

Band activity.

```yaml
messages
```

Fields:

```yaml
id

workflow_id

from_agent
to_agent

event_type

title

message

severity

created_at
```

This powers:

Band Activity page.

---

## requirements

Generated by PM Agent.

```yaml
requirements
```

Fields:

```yaml
id

project_id

requirement_id

title

description

priority

status
```

Source:

Requirements section of Reports.

---

## findings

Unified QA + Security findings.

```yaml
findings
```

Fields:

```yaml
id

workflow_id

agent_id

type:
  qa
  security

severity

title

description

status
```

Source:

QA findings + Security findings.

---

## reports

Final reviewer output.

```yaml
reports
```

Fields:

```yaml
id

workflow_id

summary

architecture_summary

implementation_summary

qa_summary

security_summary

approval_decision

generated_at
```

Source:

Reports page.

---

# Things You Should NOT Store

These should be computed:

Dashboard Stats:

```text
Active Projects
Total Agent Runs
Tasks Completed
Approval Rate
```

Compute from collections.

Do NOT create:

```yaml
dashboard_stats
```

Source:

Dashboard page.

---

# API Endpoints

For hackathon speed:

```text
GET  /projects
POST /projects

GET  /projects/:id

GET  /projects/:id/workflow

POST /projects/:id/start

GET  /workflows/:id

GET  /workflows/:id/messages

GET  /workflows/:id/agent-runs

GET  /workflows/:id/report

GET  /dashboard
```

---

# What I'd Build Tonight

### Phase 1 (1 hour)

Create Directus collections:

```text
projects
workflows
agents
agent_runs
messages
requirements
findings
reports
```

---

### Phase 2 (30 mins)

Seed:

```text
6 agents
1 demo project
1 workflow
20 messages
```

---

### Phase 3 (2 hours)

Replace hardcoded arrays in:

```text
DashboardPage
ProjectsPage
AgentWorkflowPage
BandActivityPage
ReportsPage
```

with Directus SDK calls.

---

### Phase 4 (Later)

Connect real orchestration.

---

If your submission is tomorrow, I'd actually **fake the workflow execution** first:

```text
Start Workflow
      ↓
Create Workflow
      ↓
Insert Agent Runs
      ↓
Insert Messages
      ↓
Update Progress Every 2 Seconds
```

The UI will look completely real, the judges will see agents collaborating through Band, and you can add actual LLM execution afterward if time remains. This gives you the highest chance of having a polished demo by tomorrow.
